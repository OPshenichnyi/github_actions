name: Secret Actions
on: [push]

jobs:
    run-secret-actions:
        runs-on: ubuntu-latest
        steps:
            - name: Secret Actions
              run: |
                  echo '${{ vars.STATUS_NEW_REPO}}'
                  echo '${{ secrets.PASSWORD_SECRET_ONE}}'

                 